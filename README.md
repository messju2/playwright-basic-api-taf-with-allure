This is a basic example for an API Test Automation Framework programmed in Playwright without too many bells and whistles.

Following is being used:

Node.js
Playwright
Allure (with Java)

Clone the repository
Open the project in your Favourite IDE
Run the tests that the IDE can find